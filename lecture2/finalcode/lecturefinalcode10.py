"""我们需要你开发一个“推箱子行动记录器”，暂且就叫它 sokoban.py 吧。

这个小工具主要用来记录玩家在推箱子游戏里的操作步骤，并且允许玩家后悔和重来。
具体来说，我们需要这样：
有个小本本 (history): 玩家的每一步行动都要记在这个小本本上，方便以后后悔了可以翻回去看看。
无限循环模式: 玩家可以一直玩下去，所以这个记录过程要一直进行，除非玩家主动停止。
听从指挥 (Action): 程序要时刻等着玩家输入指令，比如“上”、“下”、“左”、“右”等等，当然，我们现在先不考虑具体的上下左右，只考虑“后悔”和“重来”。
后悔药 (Undo):
如果玩家输入的是“Undo”，那就得从“小本本”里划掉最近一条记录，告诉玩家撤销了哪一步。
注意，如果“小本本”上啥都没有，那就别划了，不然会出问题的。
时光倒流 (Restart):
如果玩家输入的是“Restart”，那就把“小本本”清空，一切重新开始。
总而言之，我们希望你做一个能记录玩家操作，并且提供后悔和重置功能的推箱子辅助工具。核心是记录操作历史，撤销操作，以及重新开始。 """

history = []

while True:
    action = input("Please enter the action here: ").capitalize()
    if action == "Undo":#删除并返回列表的最后一个元素: pop() 方法会移除 history 列表中最末尾的元素（即列表中的最后一个元素），并且把这个被移除的元素作为函数的返回值，这也是很好用的删除列表元素的方法。
#将返回值赋值给变量 undo: undo = ... 这部分代码会把 pop() 方法的返回值，也就是被移除的最后一个元素，存储到变量 undo 中。
        undo = history.pop()
    elif action == "Restart":
        history.clear()
    else:
        history.append(action)
    print(f"The history of action: {history}")